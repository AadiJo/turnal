name: Release

on:
  push:
    tags:
      - "v*.*.*"
      - "!v*-nightly.*"

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - id: release_meta
        name: Resolve release metadata
        run: node scripts/resolve-release.js stable "${GITHUB_REF_NAME}"

      - name: Align package version
        run: node scripts/set-release-version.js "${{ steps.release_meta.outputs.version }}"

      - name: Verify package version
        run: |
          tag_version="${GITHUB_REF_NAME#v}"
          package_version="$(node -p "require('./package.json').version")"
          if [[ "$tag_version" != "$package_version" ]]; then
            echo "release invariant failed: tag v$tag_version does not match package.json version $package_version" >&2
            exit 1
          fi

      - name: Test
        run: npm test

      - name: Check Go formatting
        run: |
          unformatted="$(gofmt -l .)"
          if [[ -n "$unformatted" ]]; then
            echo "Go files need formatting:" >&2
            echo "$unformatted" >&2
            exit 1
          fi

      - name: Check Go modules are tidy
        run: |
          go mod tidy
          git diff --exit-code -- go.mod go.sum

      - name: Vet
        run: go vet ./...

      - name: Build
        run: npm run build

      - name: Pack dry run
        env:
          TURNAL_RELEASE_CHANNEL: ${{ steps.release_meta.outputs.channel }}
          TURNAL_COMMIT: ${{ github.sha }}
        run: npm run pack:dry-run

      - name: Generate SBOM

        uses: anchore/sbom-action@v0
        with:
          path: .
          format: spdx-json
          output-file: turnal.spdx.json
          upload-artifact: false

      - name: Publish npm package
        env:
          TURNAL_RELEASE_CHANNEL: ${{ steps.release_meta.outputs.channel }}
          TURNAL_COMMIT: ${{ github.sha }}
        run: npm publish --provenance --access public --tag "${{ steps.release_meta.outputs.npm_tag }}"

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          args=(
            "${{ steps.release_meta.outputs.tag }}"
            turnal.spdx.json
            --target "${{ github.sha }}"
            --title "${{ steps.release_meta.outputs.name }}"
            --generate-notes
          )
          if [[ "${{ steps.release_meta.outputs.prerelease }}" == "true" ]]; then
            args+=(--prerelease)
          fi
          args+=(--verify-tag)
          if [[ "${{ steps.release_meta.outputs.make_latest }}" == "false" ]]; then
            args+=(--latest=false)
          fi
          gh release create "${args[@]}"
